<%= form_for(@ambulancestatus) do |f| %>
  <% if @ambulancestatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ambulancestatus.errors.count, "error") %> prohibited this ambulancestatus from being saved:</h2>

      <ul>
      <% @ambulancestatus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
